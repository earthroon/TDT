# TDT-QMAP-STREAMING-REDUCTION-03G-R14

## Real Product QMap Execution Promotion / Live QSR03G Runtime Bridge Binding / Product-Eligible AFT00 Publication / Exact EFC Consumer Adoption / Preview·Export Direct Route / QRC02 Product Hard Denial / 1080p·4K·8K Physical Closure / Single QMap Writer Seal

## 0. Identity

```text
Patch ID = TDT-QMAP-STREAMING-REDUCTION-03G-R14
Parent = TDT-QMAP-STREAMING-REDUCTION-03G-R13
Primary purpose = promote the real QSR03G streaming QMap runtime into the normal product path
```

The previous retention-deletion interpretation is superseded. Package A remains `RELEASE_ADMITTED_NOT_DELETED`. R14 performs no quarantine, deletion or retention-release consumption.

## 1. Confirmed defect

The real `createQsr03gProductBridge()` already owns FIFO, single-flight, warm sharing, cancellation and one-restart device recovery, but its live construction is candidate-bound. Normal product boot uses a lifecycle-only shell and the R10 smoke records expected counters rather than executing the real QMap path. R5 publication also hard-codes `candidateQualificationOnly=true`, `productRouteEligible=false`, and `warmProductCacheEligible=false`.

## 2. Required terminal state

```text
global product QMap bridge = real QSR03G runtime bridge
normal Preview and Export route = QSR03G
AFT00 publication mode = PRODUCT
productRouteEligible = true
warmProductCacheEligible = true
candidateQualificationOnly = false
QRC02 normal product route = unreachable
QRC02 qualification route = retained explicitly
QRC02 automatic fallback = forbidden
```

## 3. SSOT

The global bridge key owns product-route selection. `createQsr03gProductBridge()` owns FIFO, single-flight, warm and recovery state. AFT00 owns the QMap field. R8 exact pin authority owns EFC consumption. Pipeline owns the sole final-surface publication. Preview and Export are read-only consumers.

## 4. Real product root and bridge

R14 adds a real product root, product request normalizer, product publication authority and live bridge adapter. The bridge surface includes `ensureQMapForConvergence`, `activateProduct`, `beginDrain`, `waitForQuiescence`, `invalidateDeviceEpoch`, `clearWarmEntries`, `shutdown`, and `snapshot`. A lifecycle-only object cannot satisfy R14 product admission.

## 5. Product request authority

Every request binds the installed session, operation grant, package, R10 permit, route pointer, bundle generation, product-route generation, device epoch/identity, source revision, Final EWA capability and QMap/EFC parameters. Candidate permits, qualification IDs, caller-owned GPU resources and QRC02 fallback flags are forbidden.

## 6. Product publication

Candidate publication remains unchanged and candidate-only. R14 adds a separate atomic AFT00 product publication with:

```text
candidateQualificationOnly = false
productRouteEligible = true
warmProductCacheEligible = true
normalProductBootAdmitted = true
```

Publication requires all QMap chunks and fences, a current product capability, zero intermediate readbacks, zero CPU/WebGL/Canvas pixel computation and zero validation errors.

## 7. Warm product cache

Only current product-eligible fields from the current bundle, route generation, device epoch and source revision may enter the normal warm cache. A warm delivery performs zero GPU submissions, fences and allocations, returns the same field/generation, and creates a new caller pin. Candidate fields never enter the product warm cache.

## 8. Exact EFC adoption

```text
global QSR03G bridge
→ product-eligible AFT00 QMap handle
→ exact QMap pin
→ QWave real and analytic fields on the same device
→ Bakemono live pinned-field consumption
→ one final texture
→ one Surface Registry registration
→ one Pipeline publication
→ Preview/Export consumer pins
```

Caller-supplied QMap textures, detached copies, QRC02 descriptors and duplicate final writers are rejected.

## 9. Preview and Export

Preview and Export resolve the global bridge at operation start and may not instantiate QRC02, candidate roots or runtime coordinators directly. A same-key 4K pair creates one cold leader, one joined consumer, one QMap field, one EFC graph and one final revision, with distinct operation grants and consumer pins.

## 10. QRC02 hard denial

QRC02 is denied at global route selection, request normalization, EFC descriptor admission and stale-object invocation. It remains available only behind an explicit qualification capability. Normal QRC02 requests, submissions, publications and fallback invocations must remain zero.

## 11. Canonical physical workload

```text
1080p cold = 8 submissions / 8 fences
4K cold = 21 / 21
8K cold = 75 / 75
shared 4K Preview·Export = 21 / 21
device-loss fixture = 38 submissions / 37 fences + 1 loss observation
warm replay = 0 / 0
```

Aggregate:

```text
real submissions = 163
normal fences = 162
device-loss observations = 1
terminal observations = 163
final surfaces = 6
cold QMap fields = 5
warm deliveries = 3
QRC02 product executions = 0
```

## 12. Device loss

One 4K shared operation loses the device after the ninth real submission at QMap chunk 8. The old attempt records 9 submissions and 8 fences. The replacement epoch restarts QMap at chunk zero, completes 18 QMap submissions and three QWave/EFC submissions, then an 8/8 post-loss canary passes. QRC02 fallback remains zero.

## 13. Single writer and resources

Each product operation has one AFT00 QMap writer, one Surface Registry writer and one Pipeline final writer. Preview, Export, candidate roots, lifecycle shells and QRC02 are not writers. Every terminal boundary returns FIFO/single-flight jobs, private targets, arenas, tickets, temporary pins, EFC fields, orphan surfaces and operation grants to zero.

## 14. Source and physical gates

```text
Source Gates = 432
Negative Controls = 192
Physical W-gate definitions = 96
```

W001-W096 cover real root/bridge installation, product request admission, product AFT00 publication, exact EFC binding, 1080p/4K/8K transactions, shared Preview/Export, warm replay, device loss, QRC02 denial, aggregate accounting and final promotion.

## 15. Source completion state

```text
SOURCE_BAKED_QMAP_STREAMING_REDUCTION_03G_R14_REAL_QSR03G_PRODUCT_ROOT_LIVE_PRODUCT_RUNTIME_BRIDGE_PRODUCT_ELIGIBLE_AFT00_PUBLICATION_EXACT_QMAP_QWAVE_EFC_BINDING_PREVIEW_EXPORT_DIRECT_PRODUCT_ROUTE_QRC02_PRODUCT_HARD_DENIAL_REAL_1080P_4K_8K_CANARY_PATH_WARM_PRODUCT_FIELD_ADMISSION_PRODUCT_DEVICE_LOSS_RECOVERY_SINGLE_QMAP_WRITER_NO_RETENTION_LIFECYCLE_EXPANSION_AWAITING_PACKAGED_REAL_PRODUCT_EXECUTION
```

Required source facts:

```text
Source Gates = 432/432
Negative Controls = 192/192
Physical W gates = 96 definitions
expected submissions = 163
expected normal fences = 162
expected loss observations = 1
expected final surfaces = 6
physical W gates executed = 0/96
real product promotion = false
Package A deletion = 0
```

## 16. Physical completion state

```text
PACKAGED_REAL_PRODUCT_QMAP_PROMOTION_BAKED_QMAP_STREAMING_REDUCTION_03G_R14_REAL_QSR03G_RUNTIME_BRIDGE_ACTIVE_PRODUCT_ELIGIBLE_AFT00_QMAP_PUBLICATION_EXACT_QMAP_QWAVE_EFC_LIVE_RESOURCE_ADOPTION_PREVIEW_EXPORT_DIRECT_QSR03G_ROUTE_REAL_1080P_4K_8K_PRODUCT_EXECUTION_WARM_PRODUCT_QMAP_SHARING_ONE_DEVICE_LOSS_RECOVERY_ZERO_QRC02_PRODUCT_EXECUTION_ONE_QMAP_FIELD_WRITER_ONE_FINAL_SURFACE_WRITER_ZERO_PRIVATE_RESOURCE_LEAK_REAL_QSR03G_PRODUCT_QMAP_PROMOTED
```

## 17. Final seal

R14 does not delete Package A. R14 promotes the actual QMap engine. The real FIFO, single-flight, warm-cache, cancellation and device-loss bridge becomes the normal product runtime. AFT00 product fields are admitted by current product authority and consumed by exact EFC pins. Preview and Export share the same global QSR03G route. Synthetic lifecycle bridges and synthetic smoke counters have no physical promotion authority. Physical promotion requires real queue submissions, real fences, real field publication, real EFC convergence, real warm sharing and real device-loss recovery.
