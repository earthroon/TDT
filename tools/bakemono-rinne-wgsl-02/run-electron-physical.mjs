import fs from 'node:fs';import path from 'node:path';import { spawnSync } from 'node:child_process';import { fileURLToPath } from 'node:url';
const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../..');const OUT=path.join(ROOT,'artifacts/bakemono-rinne-wgsl-02/physical/electron-result.json');const electron=path.join(ROOT,'node_modules','.bin',process.platform==='win32'?'electron.cmd':'electron');fs.mkdirSync(path.dirname(OUT),{recursive:true});
if(!fs.existsSync(electron)){fs.writeFileSync(OUT,JSON.stringify({status:'PENDING',reason:'ELECTRON_DEPENDENCY_UNAVAILABLE',physicalGateCount:48,passCount:0,pendingCount:48},null,2)+'\n');console.log('PENDING electron dependency unavailable');process.exit(0);}
const main=path.join(ROOT,'tools/bakemono-rinne-wgsl-02/electron-qualification-main.mjs');const run=spawnSync(electron,[main],{cwd:ROOT,env:{...process.env,BKR02_PHYSICAL_RESULT:OUT},stdio:'inherit'});if(run.error)throw run.error;process.exitCode=run.status??2;
