import fs from 'node:fs';import path from 'node:path';import {runArtifact,readExternalJson,check,runId} from './lib.mjs';
export function enterMaintenance(){const drain=readExternalJson('DADUM_R10_DRAIN_RECEIPT','E_R10_MAINTENANCE_DRAIN_TIMEOUT');const d=drain.value;for(const k of ['pendingJobs','pendingReadbacks','pendingSaveSessions','activeWorkerJobs'])check(d[k]===0,'E_R10_PENDING_JOB_NONZERO',`${k} nonzero`,d[k]);check(d.newJobAdmissionBlocked===true,'E_R10_MAINTENANCE_DRAIN_TIMEOUT','new job admission not blocked');check(d.activeProcessExited===true,'E_R10_MAINTENANCE_DRAIN_TIMEOUT','active process not quiescent');const receipt={schemaVersion:1,runId:runId(),state:'DRAINED',drainReceiptPath:drain.path,drainReceiptSha256:drain.sha256,pendingJobs:0,pendingReadbacks:0,pendingSaveSessions:0,activeWorkerJobs:0,newJobAdmissionBlocked:true,activeProcessExited:true,forcedKillUsed:Boolean(d.forcedKillUsed)};runArtifact('R10_MAINTENANCE_DRAIN_RECEIPT.json',receipt);return receipt;}
