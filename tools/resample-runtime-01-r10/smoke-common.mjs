import {readExternalJson,check,runArtifact,runId} from './lib.mjs';import {readPointerSet} from './pointer-reader.mjs';
export function verifySmoke(envName,receiptName,errorCode){const x=readExternalJson(envName,errorCode);const r=x.value,p=readPointerSet().pointer;check(r.pass===true,errorCode,'smoke did not pass');check(r.buildId===p.activeBuildId&&r.packageContentId===p.activePackageContentId,errorCode,'smoke identity mismatch');for(const k of ['previewSmokePassed','exportSmokePassed','previewExportIdentityPassed','alphaBorderDcPassed','validationCountersZero','cpuFallbackUsed']){if(k==='cpuFallbackUsed')check(r[k]===false,errorCode,'CPU fallback used');else check(r[k]===true,errorCode,`${k} false`);}check(r.pendingJobs===0,errorCode,'pending jobs after smoke');const out={schemaVersion:1,runId:runId(),...r,sourceReceiptPath:x.path,sourceReceiptSha256:x.sha256};runArtifact(receiptName,out);return out;}
