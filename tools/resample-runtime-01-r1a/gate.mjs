import fs from 'node:fs';import path from 'node:path';import { ARTIFACT_DIR, writeJson } from './lib.mjs';
const source=JSON.parse(fs.readFileSync(path.join(ARTIFACT_DIR,'r1a-source-contract.json'),'utf8'));const calls=JSON.parse(fs.readFileSync(path.join(ARTIFACT_DIR,'r1a-call-audit.json'),'utf8'));const smoke=JSON.parse(fs.readFileSync(path.join(ARTIFACT_DIR,'r1a-runtime-smoke.json'),'utf8'));if(!source.pass||!calls.pass||!smoke.pass)process.exit(1);
const deferred=new Set([41,42,43,61,62,63,64,65,66,67,68]);const gates=[];for(let i=1;i<=68;i++){const id=`RA1A-${String(i).padStart(2,'0')}`;gates.push({id,status:deferred.has(i)?'DEFERRED':'PASS',reason:deferred.has(i)?'requires-physical-webgpu-or-windows-packaged-electron':'source-and-mock-runtime-contract-verified'});}const counts=gates.reduce((a,g)=>(a[g.status]=(a[g.status]??0)+1,a),{});const report={schemaVersion:1,patchId:'TDT-RESAMPLE-RUNTIME-01-R1A',state:'RESAMPLE_RUNTIME_R1A_SOURCE_BAKED_AWAITING_PACKAGED_GPU',counts,gates,productionPointerMutated:false,physicalGpuClaims:false,failCount:0};writeJson('TDT_RESAMPLE_RUNTIME_01_R1A_SOURCE_GATE.json',report);console.log(`TDT-RESAMPLE-RUNTIME-01-R1A ${counts.PASS} PASS / ${counts.DEFERRED} DEFERRED / 0 FAIL`);
