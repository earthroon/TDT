import {json,write,sha256,canonical} from './lib.mjs';const source=json('artifacts/hannakairo-gate-02/source-bake/hg02-source-contract.json'),wgsl=json('artifacts/hannakairo-gate-02/source-bake/hg02-wgsl-contract.json'),smoke=json('artifacts/hannakairo-gate-02/source-bake/hg02-runtime-smoke.json');const evidence=[...source.checks,...wgsl.checks,...smoke.checks];if(evidence.length!==172)throw new Error(`E_HG02_GATE_COUNT:${evidence.length}`);const gates=[];for(let i=1;i<=184;i++){const gateId=`HG02-${String(i).padStart(3,'0')}`;if(i>=165&&i<=176)gates.push({gateId,status:'DEFERRED',detail:'Physical WebGPU / Packaged Electron evidence required'});else{const e=evidence.shift();gates.push({gateId,status:e.pass?'PASS':'FAIL',detail:e.detail||e.name});}}const pass=gates.filter(x=>x.status==='PASS').length,deferred=gates.filter(x=>x.status==='DEFERRED').length,fail=gates.filter(x=>x.status==='FAIL').length;const unsigned={schemaVersion:1,patchId:'TDT-HANNAKAIRO-GATE-02',state:fail?'FAILED':'HANNAKAIRO_GATE_02_SOURCE_BAKED_AWAITING_PACKAGED_GPU',pass,deferred,fail,failCount:fail,counts:{PASS:pass,DEFERRED:deferred,FAIL:fail},gates,physicalGpuClaims:false,productionPointerMutation:false};const report={...unsigned,gateDigest:sha256(canonical(unsigned))};write('TDT_HANNAKAIRO_GATE_02_SOURCE_GATE.json',report);console.log(`TDT-HANNAKAIRO-GATE-02 ${pass} PASS / ${deferred} DEFERRED / ${fail} FAIL`);if(fail)process.exit(1);
