import {spawnSync} from 'node:child_process';import path from 'node:path';import {fileURLToPath} from 'node:url';const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../..');for(const script of ['run-authority-unit.mjs','verify-negative-controls.mjs','gate-physical.mjs','verify-source.mjs']){const result=spawnSync(process.execPath,[path.join(ROOT,'tools/bakemono-rinne-wgsl-06',script)],{cwd:ROOT,stdio:'inherit'});if(result.status!==0)process.exit(result.status??1);}console.log('PASS TDT-BAKEMONO-RINNE-WGSL-06 SOURCE_BAKED_AWAITING_QMAP_PRODUCER_AND_PHYSICAL_GPU');
