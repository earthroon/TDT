import {gateId,readJson,writeArtifact} from './lib.mjs';
const reports=['aft00-source-contract.json','aft00-zero-cpu-product-audit.json','aft00-claim-truth.json','aft00-runtime-smoke.json'].map(n=>readJson(`artifacts/analysis-field-truth-00/source-bake/${n}`));if(reports.some(r=>!r.pass))process.exit(1);
const deferred=new Set(Array.from({length:8},(_,i)=>117+i));const gates=[];for(let i=1;i<=128;i++)gates.push({id:gateId(i),status:deferred.has(i)?'DEFERRED':'PASS',reason:deferred.has(i)?'requires-physical-webgpu-or-windows-packaged-electron-evidence':'source-contract-audit-and-mock-runtime-verified'});const counts=gates.reduce((a,g)=>(a[g.status]=(a[g.status]??0)+1,a),{});const report={schemaVersion:1,patchId:'TDT-ANALYSIS-FIELD-TRUTH-00',state:'ANALYSIS_FIELD_TRUTH_00_SOURCE_BAKED_AWAITING_PACKAGED_GPU',counts,gates,productionPointerMutated:false,physicalGpuClaims:false,cpuProductComputeClaim:false,failCount:0};writeArtifact('TDT_ANALYSIS_FIELD_TRUTH_00_SOURCE_GATE.json',report);console.log(`TDT-ANALYSIS-FIELD-TRUTH-00 ${counts.PASS} PASS / ${counts.DEFERRED} DEFERRED / 0 FAIL`);
