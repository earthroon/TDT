import fs from 'node:fs';import path from 'node:path';import req from './gate-requirements.json' with {type:'json'};import {ARTIFACT_DIR,writeJson} from './lib.mjs';const needed=['TDT_RESAMPLE_RUNTIME_01_R5_ORACLE_RECEIPT.json','TDT_RESAMPLE_RUNTIME_01_R5_SOURCE_CONTRACT_RECEIPT.json','TDT_RESAMPLE_RUNTIME_01_R5_MOCK_RUNTIME_RECEIPT.json','TDT_RESAMPLE_RUNTIME_01_R5_PREDECESSOR_REGRESSION_RECEIPT.json'];const reports=needed.map(n=>JSON.parse(fs.readFileSync(path.join(ARTIFACT_DIR,n),'utf8')));const pass=reports.every(r=>r.pass);const gates=req.map((requirement,i)=>({id:`R5-${String(i+1).padStart(3,'0')}`,requirement,status:i>=132?'DEFERRED':pass?'PASS':'FAIL',reason:i>=132?'requires-physical-webgpu-or-packaged-electron-evidence':'source-axial-oracle-runtime-and-regression-evidence-verified'}));const counts=gates.reduce((a,g)=>(a[g.status]=(a[g.status]||0)+1,a),{});const report={schemaVersion:1,patchId:'TDT-RESAMPLE-RUNTIME-01-R5',state:pass?'RESAMPLE_RUNTIME_R5_AXIAL_SUBPIXEL_CONTINUITY_SEALED_AWAITING_R6':'RESAMPLE_RUNTIME_R5_SOURCE_GATE_FAILED',counts,gates,productionPointerMutated:false,physicalGpuExecuted:false,packagedElectronVerified:false,failCount:counts.FAIL||0};writeJson('TDT_RESAMPLE_RUNTIME_01_R5_SOURCE_GATE.json',report);if(!pass)process.exit(1);console.log(`TDT-RESAMPLE-RUNTIME-01-R5 ${counts.PASS} PASS / ${counts.DEFERRED} DEFERRED / ${counts.FAIL||0} FAIL`);
