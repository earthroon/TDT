import fs from 'node:fs';import path from 'node:path';import childProcess from 'node:child_process';import {sourceArtifact,seal,check} from './lib.mjs';const dir='tools/build-lock-01-r2';const files=fs.readdirSync(dir).filter(x=>x.endsWith('.mjs')).sort();const rows=[];for(const name of files){const r=childProcess.spawnSync(process.execPath,['--check',path.join(dir,name)],{encoding:'utf8'});rows.push({file:path.join(dir,name),status:r.status===0?'PASS':'FAIL',stderr:r.stderr});}check(rows.every(x=>x.status==='PASS'),'E_BUILD_LOCK_R2_FINAL_CHILD_MISSING','JavaScript parse failed',rows.filter(x=>x.status==='FAIL'));sourceArtifact('BLR2_JAVASCRIPT_PARSE_REPORT.json',seal({schemaVersion:1,passCount:rows.length,failCount:0,rows}));console.log('BLR2 JavaScript parse PASS '+rows.length+'/'+rows.length);
