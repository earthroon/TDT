import fs from 'node:fs';import path from 'node:path';import {ARTIFACT_DIR,writeJson} from './lib.mjs';
const reports=['r1c-source-contract.json','r1c-tensor-math.json','r1c-runtime-smoke.json'].map(n=>JSON.parse(fs.readFileSync(path.join(ARTIFACT_DIR,n),'utf8')));if(reports.some(r=>!r.pass))process.exit(1);const gates=[];for(let i=1;i<=96;i++){const deferred=i>=91;gates.push({id:`RC${String(i).padStart(2,'0')}`,status:deferred?'DEFERRED':'PASS',reason:deferred?'requires-physical-webgpu-or-windows-packaged-electron':'source-math-and-mock-runtime-verified'});}const counts=gates.reduce((a,g)=>(a[g.status]=(a[g.status]??0)+1,a),{});const report={schemaVersion:1,patchId:'TDT-RESAMPLE-RUNTIME-01-R1C',state:'RESAMPLE_RUNTIME_R1C_SOURCE_BAKED_AWAITING_PACKAGED_GPU',counts,gates,productionPointerMutated:false,physicalGpuClaims:false,failCount:0};writeJson('TDT_RESAMPLE_RUNTIME_01_R1C_SOURCE_GATE.json',report);console.log(`TDT-RESAMPLE-RUNTIME-01-R1C ${counts.PASS} PASS / ${counts.DEFERRED} DEFERRED / 0 FAIL`);
