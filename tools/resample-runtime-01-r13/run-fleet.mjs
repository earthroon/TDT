import fs from 'node:fs';import path from 'node:path';import {ROOT,FLEET,ensure,check,writeJson} from './lib.mjs';
const input=process.env.DADUM_R13_FLEET_INPUT;check(input&&fs.existsSync(input),'E_R13_FLEET_NOT_QUALIFIED','DADUM_R13_FLEET_INPUT is required and must point to a qualified multi-installation fleet input bundle');const bundle=JSON.parse(fs.readFileSync(input,'utf8'));check(bundle.schemaId==='tdt.rollout.fleet-execution-input.r13.v1','E_R13_FLEET_NOT_QUALIFIED','fleet input schema mismatch');check(Array.isArray(bundle.installations)&&bundle.installations.length>=2,'E_R13_FLEET_NOT_QUALIFIED','at least two qualified installations are required');check(bundle.r10TargetReleaseState==='RESAMPLE_RUNTIME_R10_PRODUCTION_RELEASE_SEALED','E_R13_FLEET_NOT_QUALIFIED','R10 target release not sealed');check(bundle.installations.every(i=>i.r11State==='RESAMPLE_RUNTIME_R11_INSTALLED_RUNTIME_ATTESTATION_AND_QUARANTINE_SEALED'&&i.r12State==='RESAMPLE_RUNTIME_R12_ATOMIC_UPDATE_INSTALLATION_AND_ATTESTATION_HANDOFF_SEALED'),'E_R13_FLEET_NOT_QUALIFIED','R11/R12 installed predecessor incomplete');ensure(FLEET);writeJson(path.join(FLEET,'R13_FLEET_PREDECESSOR_ADMISSION_RECEIPT.json'),{schemaVersion:1,schemaId:'tdt.rollout.fleet-predecessor-admission.r13.v1',qualifiedInstallations:bundle.installations.length,sourceBundle:path.basename(input),pass:true});console.log('R13 fleet predecessor admitted; execute signed ring workflow and provide the remaining fleet receipts before finalization');
