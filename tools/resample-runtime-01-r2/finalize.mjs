import fs from 'node:fs';import path from 'node:path';import {ROOT,ARTIFACT_DIR,PATCH_ID,sha256,sha256File,writeJson} from './lib.mjs';const changedManifestRelative='patches/TDT_RESAMPLE_RUNTIME_01_R2_CHANGED_FILE_MANIFEST.json';const patchRelative='patches/TDT_RESAMPLE_RUNTIME_01_R2_workgroup_tiled_ewa_optimization_uniform_barrier_baseline_pixel_parity.diff';const regressionRelative='artifacts/resample-runtime-01-r2/source-bake/TDT_RESAMPLE_RUNTIME_01_R2_REGRESSION_SUMMARY.json';const changed=JSON.parse(fs.readFileSync(path.join(ROOT,changedManifestRelative),'utf8'));const files=[...changed.files.filter(e=>e.status!=='deleted').map(e=>e.relative),changedManifestRelative,patchRelative,regressionRelative].filter((r,i,a)=>a.indexOf(r)===i&&fs.existsSync(path.join(ROOT,r))).sort();const records=files.map(relative=>({relative,byteLength:fs.statSync(path.join(ROOT,relative)).size,sha256:sha256File(relative)}));const gate=JSON.parse(fs.readFileSync(path.join(ARTIFACT_DIR,'TDT_RESAMPLE_RUNTIME_01_R2_SOURCE_GATE.json'),'utf8'));const sourceSeal=sha256(JSON.stringify({patchId:PATCH_ID,parentZipSha256:changed.parentZipSha256,records,counts:gate.counts}));writeJson('TDT_RESAMPLE_RUNTIME_01_R2_SOURCE_RECEIPT.json',{schemaVersion:1,patchId:PATCH_ID,state:gate.state,parentPatchId:'TDT-RESAMPLE-RUNTIME-01-R1D',parentZipSha256:changed.parentZipSha256,sourceSealSha256:sourceSeal,changedFileCount:changed.changedFileCount,sealedFileCount:records.length,files:records,gateCounts:gate.counts,productionPointerMutated:false,physicalGpuClaims:false,exactPixelParityClaim:false,performanceClaim:false,windowsPackagedElectronClaims:false});console.log(`PASS R2 source seal ${sourceSeal} changed=${changed.changedFileCount} sealed=${records.length}`);
