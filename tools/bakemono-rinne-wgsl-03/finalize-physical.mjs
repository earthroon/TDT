import fs from 'node:fs';import path from 'node:path';import {createHash} from 'node:crypto';import {fileURLToPath} from 'node:url';
const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../..');const rel='artifacts/bakemono-rinne-wgsl-03/physical/physical-gate-report.json';const report=JSON.parse(fs.readFileSync(path.join(ROOT,rel),'utf8'));const body={schemaVersion:1,schemaId:'tdt.effect.bakemono-rinne.wgsl03-physical-final-receipt.v1',patchId:'TDT-BAKEMONO-RINNE-WGSL-03',status:report.status,physicalGateReportDigest:createHash('sha256').update(fs.readFileSync(path.join(ROOT,rel))).digest('hex'),outputAuthority:'CANONICAL_CANDIDATE_SHADOW_ONLY',canonicalFinalTextureClaim:false};const receipt={...body,selfSha256:createHash('sha256').update(JSON.stringify(body)).digest('hex')};fs.writeFileSync(path.join(ROOT,'artifacts/bakemono-rinne-wgsl-03/physical/physical-final-receipt.json'),JSON.stringify(receipt,null,2)+'\n');console.log(JSON.stringify(receipt,null,2));if(report.status!=='PASS')process.exitCode=2;
