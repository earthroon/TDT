import fs from 'node:fs';import path from 'node:path';import { ARTIFACT_DIR, writeJson } from './lib.mjs';import requirements from './gate-requirements.json' with { type: 'json' };
const reports=['r4-parent-freeze.json','TDT_RESAMPLE_RUNTIME_01_R4_COORDINATE_REPAIR_RECEIPT.json','TDT_RESAMPLE_RUNTIME_01_R4_SHADER_IDENTITY_RECEIPT.json','TDT_RESAMPLE_RUNTIME_01_R4_PHASE_AWARE_TILE_PROOF_RECEIPT.json','TDT_RESAMPLE_RUNTIME_01_R4_R3_ORACLE_RELATIONSHIP_RECEIPT.json','TDT_RESAMPLE_RUNTIME_01_R4_RUNTIME_WIRING_RECEIPT.json','TDT_RESAMPLE_RUNTIME_01_R4_ZERO_FALLBACK_RECEIPT.json','TDT_RESAMPLE_RUNTIME_01_R4_MOCK_RUNTIME_RECEIPT.json','r4-predecessor-regression.json'].map((name)=>JSON.parse(fs.readFileSync(path.join(ARTIFACT_DIR,name),'utf8')));const sourcePass=reports.every((report)=>report.pass!==false);const gates=requirements.map((requirement,index)=>{const number=index+1;const deferred=number>=103;return {id:`R4-${String(number).padStart(3,'0')}`,requirement,status:deferred?'DEFERRED':sourcePass?'PASS':'FAIL',reason:deferred?'requires-physical-webgpu-or-packaged-electron-evidence':'source-coordinate-tile-runtime-and-regression-evidence-verified'};});const counts=gates.reduce((acc,gate)=>(acc[gate.status]=(acc[gate.status]??0)+1,acc),{});const report={schemaVersion:1,patchId:'TDT-RESAMPLE-RUNTIME-01-R4',state:sourcePass?'RESAMPLE_RUNTIME_R4_PHASE_CORRECT_PRODUCT_REFERENCE_REPAIRED_AWAITING_R5':'RESAMPLE_RUNTIME_R4_SOURCE_GATE_FAILED',counts,gates,productionPointerMutated:false,physicalGpuExecuted:false,packagedElectronVerified:false,failCount:counts.FAIL??0};writeJson('TDT_RESAMPLE_RUNTIME_01_R4_SOURCE_GATE.json',report);if(!sourcePass)process.exit(1);console.log(`TDT-RESAMPLE-RUNTIME-01-R4 ${counts.PASS} PASS / ${counts.DEFERRED} DEFERRED / ${counts.FAIL??0} FAIL`);
