import fs from 'node:fs'; import path from 'node:path'; import { DIST,check,verifySelf } from './lib.mjs'; import { FINAL_STATE,DISTRIBUTION_PENDING } from './identity.mjs';
const finalPath=path.join(DIST,'TDT_RESAMPLE_RUNTIME_01_R14A_DISTRIBUTION_FINAL_RECEIPT.json'); if(!fs.existsSync(finalPath)) throw Object.assign(new Error('R14A distribution final receipt is missing. Run offline root signing, transparency publication, witness quorum, mirror/CDN verification, R12A admission and R13A binding first.'),{code:'E_R14A_DISTRIBUTION_RECEIPT_MISSING'}); const receipt=JSON.parse(fs.readFileSync(finalPath,'utf8')); check((verifySelf(receipt,'finalReceiptSha256')||verifySelf(receipt,'selfSha256'))&&receipt.state===FINAL_STATE,'E_R14A_FINALIZER_REVALIDATION_FAILED','R14A distribution final receipt invalid'); check(receipt.counts?.PASS===1400&&receipt.counts?.FAIL===0,'E_R14A_FINALIZER_REVALIDATION_FAILED','R14A distribution counts invalid'); const gatePath=path.join(DIST,'R14A_DISTRIBUTION_GATE_REPORT.json'); check(fs.existsSync(gatePath),'E_R14A_DISTRIBUTION_RECEIPT_MISSING','R14A distribution gate report missing'); const gate=JSON.parse(fs.readFileSync(gatePath,'utf8')); check(gate.counts?.PASS===DISTRIBUTION_PENDING&&gate.counts?.FAIL===0,'E_R14A_FINALIZER_REVALIDATION_FAILED','R14A distribution gate report invalid'); console.log(`TDT-RESAMPLE-RUNTIME-01-R14A 640 SOURCE PASS / ${DISTRIBUTION_PENDING} DISTRIBUTION PASS / 0 FAIL`);
