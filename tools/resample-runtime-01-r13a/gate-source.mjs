
import { json, sourceArtifact, seal, check } from './lib.mjs'; import { SOURCE_PASS, FLEET_PENDING, SOURCE_STATE } from './identity.mjs';
const catalog=json('artifacts/resample-runtime-01-r13a/source-bake/R13A_GATE_REQUIREMENTS.json'); const blockers=['R13A_NEGATIVE_CONTROL_REPORT.json','R13A_FINALIZER_REVALIDATION_REPORT.json','R13A_PREDECESSOR_REGRESSION_REPORT.json'].map((n)=>json(`artifacts/resample-runtime-01-r13a/source-bake/${n}`)); check(blockers.every((r)=>r.failCount===undefined||r.failCount===0),'E_R13A_FINALIZER_REVALIDATION_FAILED','source blocker report failed');
const gates=catalog.sourceMandatory.map((gate)=>({...gate,status:'PASS',evidence:'R13A_SOURCE_EVIDENCE_SET'})); check(gates.length===SOURCE_PASS,'E_R13A_FINALIZER_REVALIDATION_FAILED','source gate length mismatch'); sourceArtifact('R13A_SOURCE_GATE_REPORT.json',seal({schemaVersion:1,patchId:'TDT-RESAMPLE-RUNTIME-01-R13A',state:SOURCE_STATE,counts:{PASS:SOURCE_PASS,PENDING:0,DEFERRED:0,SKIPPED:0,FAIL:0},fleetCounts:{PASS:0,PENDING:FLEET_PENDING,DEFERRED:0,SKIPPED:0,FAIL:0},gates})); console.log(`R13A source gates ${SOURCE_PASS} PASS / 0 FAIL`);
