import fs from 'node:fs';import path from 'node:path';import { fileURLToPath } from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../..');const report=JSON.parse(fs.readFileSync(path.join(root,'artifacts/bakemono-rinne-wgsl-04/physical/physical-gate-report.json'),'utf8'));console.log(`BKR04 PHYSICAL ${report.status} ${report.passCount}/${report.gateCount}`);if(report.status==='FAIL')process.exitCode=1;
