import {read,check} from './lib.mjs';import {SPEC,SOURCE_PASS,PACKAGED_PENDING} from './identity.mjs';
export function parseGateCatalog(){const text=read(SPEC);const source=[...text.matchAll(/`(R9AP1R2-S\d{3})`\s*\|\s*([^|\n]+)/g)].map(m=>({id:m[1],requirement:m[2].trim()}));const packaged=[...text.matchAll(/`(R9AP1R2-P\d{3})`\s*\|\s*([^|\n]+)/g)].map(m=>({id:m[1],requirement:m[2].trim()}));check(source.length===SOURCE_PASS,'E_R9AP1R2_GATE_CATALOG','source gate count mismatch',{actual:source.length});check(packaged.length===PACKAGED_PENDING,'E_R9AP1R2_GATE_CATALOG','packaged gate count mismatch',{actual:packaged.length});check(new Set(source.map(r=>r.id)).size===source.length,'E_R9AP1R2_GATE_DUPLICATE','duplicate source gate');check(new Set(packaged.map(r=>r.id)).size===packaged.length,'E_R9AP1R2_GATE_DUPLICATE','duplicate packaged gate');return Object.freeze({source,packaged});}
