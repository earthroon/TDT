import {json,write,sha256,canonical} from './lib.mjs';const source=json('artifacts/qwave-phase-03/source-bake/qp03-source-contract.json'),wgsl=json('artifacts/qwave-phase-03/source-bake/qp03-wgsl-contract.json'),smoke=json('artifacts/qwave-phase-03/source-bake/qp03-runtime-smoke.json');const evidence=[...source.checks,...wgsl.checks,...smoke.checks];if(evidence.length!==192)throw new Error(`E_QP03_GATE_COUNT:${evidence.length}`);const gates=[];for(let i=1;i<=204;i++){const gateId=`QP03-${String(i).padStart(3,'0')}`;if(i>=189&&i<=200)gates.push({gateId,status:'DEFERRED',detail:'Physical WebGPU / Packaged Electron evidence required'});else{const e=evidence.shift();gates.push({gateId,status:e.pass?'PASS':'FAIL',detail:e.detail||e.name});}}const pass=gates.filter(x=>x.status==='PASS').length,deferred=gates.filter(x=>x.status==='DEFERRED').length,fail=gates.filter(x=>x.status==='FAIL').length;const unsigned={schemaVersion:1,patchId:'TDT-QWAVE-PHASE-03',state:fail?'FAILED':'QWAVE_PHASE_03_SOURCE_BAKED_AWAITING_PACKAGED_GPU',pass,deferred,fail,failCount:fail,counts:{PASS:pass,DEFERRED:deferred,FAIL:fail},gates,physicalGpuClaims:false,productionPointerMutation:false};const report={...unsigned,gateDigest:sha256(canonical(unsigned))};write('TDT_QWAVE_PHASE_03_SOURCE_GATE.json',report);console.log(`TDT-QWAVE-PHASE-03 ${pass} PASS / ${deferred} DEFERRED / ${fail} FAIL`);if(fail)process.exit(1);
