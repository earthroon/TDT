import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';import {fileURLToPath} from 'node:url';
export const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../..');export const outDir=path.join(root,'artifacts/qwave-phase-03/source-bake');fs.mkdirSync(outDir,{recursive:true});
export const read=(p)=>fs.readFileSync(path.join(root,p),'utf8');export const json=(p)=>JSON.parse(read(p));export const exists=(p)=>fs.existsSync(path.join(root,p));export const sha256=(b)=>crypto.createHash('sha256').update(b).digest('hex');export const sha256File=(p)=>sha256(fs.readFileSync(path.join(root,p)));export const canonical=(v)=>JSON.stringify(sort(v));function sort(v){if(Array.isArray(v))return v.map(sort);if(v&&typeof v==='object')return Object.fromEntries(Object.keys(v).sort().map(k=>[k,sort(v[k])]));return v;}export function write(name,value){fs.writeFileSync(path.join(outDir,name),JSON.stringify(value,null,2)+'\n');}export function f32bits(value){const a=new Float32Array([value]);return new Uint32Array(a.buffer)[0];}
