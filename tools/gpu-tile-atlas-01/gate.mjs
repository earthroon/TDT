import {json,write,sha256,canonical} from './lib.mjs';const source=json('artifacts/gpu-tile-atlas-01/source-bake/gta01-source-contract.json'),wgsl=json('artifacts/gpu-tile-atlas-01/source-bake/gta01-wgsl-contract.json'),smoke=json('artifacts/gpu-tile-atlas-01/source-bake/gta01-runtime-smoke.json');const evidence=[...source.checks,...wgsl.checks,...smoke.checks];if(evidence.length!==212)throw new Error(`E_GTA01_GATE_COUNT:${evidence.length}`);const gates=[];for(let i=1;i<=224;i++){const gateId=`GTA01-${String(i).padStart(3,'0')}`;if(i>=209&&i<=220)gates.push({gateId,status:'DEFERRED',detail:'Physical WebGPU / Windows x64 Packaged Electron evidence required'});else{const e=evidence.shift();gates.push({gateId,status:e.pass?'PASS':'FAIL',detail:e.detail||e.name});}}const pass=gates.filter(x=>x.status==='PASS').length,deferred=gates.filter(x=>x.status==='DEFERRED').length,fail=gates.filter(x=>x.status==='FAIL').length;const unsigned={schemaVersion:1,patchId:'TDT-GPU-TILE-ATLAS-01',state:fail?'FAILED':'GPU_TILE_ATLAS_01_SOURCE_BAKED_AWAITING_PACKAGED_GPU',pass,deferred,fail,counts:{PASS:pass,DEFERRED:deferred,FAIL:fail},gates,physicalGpuClaims:false,productionPointerMutation:false};const report={...unsigned,gateDigest:sha256(canonical(unsigned))};write('TDT_GPU_TILE_ATLAS_01_SOURCE_GATE.json',report);console.log(`TDT-GPU-TILE-ATLAS-01 ${pass} PASS / ${deferred} DEFERRED / ${fail} FAIL`);if(fail)process.exit(1);
