import {writeFixture,report,capture,check} from './lib.mjs';
import {computeEwaSupportEnvelopeR8,policyFootprintBoundR8} from '../../app/legacy-runtime/core/compute/qmap_webgpu/ewa_support_envelope_r8.mjs';
const scales=[1,1.125,1.5,1.75,2];const anisotropies=[1,2,3,4];const policyBounds=[.75,1,1.5];
const support=[];for(const scale of scales)for(const maxAnisotropy of anisotropies)for(const policyFootprintBound of policyBounds){const out=128,src=Math.round(out*scale);const e=computeEwaSupportEnvelopeR8({sourceWidth:src,sourceHeight:src,outputWidth:out,outputHeight:out,sigmaMain:1.25,sigmaCross:.65,maxAnisotropy,policyFootprintBound});support.push({scale,maxAnisotropy,policyFootprintBound,majorBound:e.majorBound,minorBound:e.minorBound,requiredReach:e.requiredReach});}
const alpha=[{name:'opaque-red',rgba:[1,0,0,1]},{name:'half-green-straight',rgba:[0,1,0,.5]},{name:'half-green-premult',rgba:[0,.5,0,.5]},{name:'hidden-rgb-transparent',rgba:[1,.2,.7,0]},{name:'low-alpha',rgba:[.1,.05,.02,1e-8]}];
const border=[{size:[1,1],positions:[[0,0],[.25,.75]]},{size:[1,17],positions:[[0,0],[0,8.5],[0,16]]},{size:[19,1],positions:[[0,0],[9.25,0],[18,0]]},{size:[17,19],positions:[[0,0],[16,18],[.125,9.75]]}];
writeFixture('support-envelope-fixtures.json',{schemaVersion:1,support});writeFixture('alpha-conservation-fixtures.json',{schemaVersion:1,alpha});writeFixture('border-dc-fixtures.json',{schemaVersion:1,border});
const checks=[capture('support-fixture-count',()=>{check(support.length===60,'E_R8_FIXTURE_COUNT','support fixture count');return support.length;}),capture('alpha-fixture-count',()=>{check(alpha.length>=5,'E_R8_FIXTURE_COUNT','alpha fixture count');return alpha.length;}),capture('policy-bound',()=>policyFootprintBoundR8({level0FootprintScale:.75,level1FootprintScale:1.5,level2FootprintScale:1}))];
report('TDT_RESAMPLE_RUNTIME_01_R8_FIXTURE_REPORT.json',checks,{supportFixtureCount:support.length,alphaFixtureCount:alpha.length,borderFixtureCount:border.length});if(checks.some(x=>!x.pass))process.exit(1);
