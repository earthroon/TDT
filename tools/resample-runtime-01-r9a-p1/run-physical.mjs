import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';import {spawnSync} from 'node:child_process';import {check,ROOT} from './lib.mjs';
check(process.platform==='win32'&&process.arch==='x64','E_R9AP1_WIN32_X64_REQUIRED','R9A-P1 physical run requires win32-x64');const bl=path.join(ROOT,'artifacts/build-lock-01-r2/win32/TDT_BUILD_LOCK_01_R2_FINAL_ADMISSION_RECEIPT.json');check(fs.existsSync(bl),'E_BUILD_LOCK_R2_WIN32_RECEIPT_MISSING','Build Lock R2 final receipt missing');const exe=process.env.DADUM_R9A_P1_EXE||path.join(ROOT,'release','win-unpacked','DadumDadum.exe');check(fs.existsSync(exe),'E_R9AP1_PACKAGED_EXE_MISSING','packaged executable missing',{exe});const runId=crypto.randomBytes(32).toString('hex');const evidenceRoot=path.resolve(process.env.DADUM_R9A_P1_EVIDENCE_ROOT||path.join(ROOT,'artifacts/resample-runtime-01-r9a-p1/physical-runs'));const result=spawnSync(exe,[],{stdio:'inherit',env:{...process.env,DADUM_R9A_P1_PHYSICAL_MODE:'1',DADUM_R9A_P1_RUN_ID:runId,DADUM_R9A_P1_EVIDENCE_ROOT:evidenceRoot,DADUM_R9A_P1_ADAPTER_ROLE:process.env.DADUM_R9A_P1_ADAPTER_ROLE||'performance-canonical'}});process.exit(result.status??1);
