import {gateId,readJson,writeArtifact} from './lib.mjs';
const reports=['sq02-source-contract.json','sq02-wgsl-contract.json','sq02-runtime-smoke.json'].map(n=>readJson(`artifacts/spectral-qmap-02/source-bake/${n}`));if(reports.some(r=>!r.pass))process.exit(1);
const deferred=new Set(Array.from({length:12},(_,i)=>157+i));const gates=[];for(let i=1;i<=180;i++)gates.push({id:gateId(i),status:deferred.has(i)?'DEFERRED':'PASS',reason:deferred.has(i)?'requires-physical-webgpu-or-windows-packaged-electron-evidence':'source-contract-wgsl-audit-and-deterministic-mock-verified'});const counts=gates.reduce((a,g)=>(a[g.status]=(a[g.status]??0)+1,a),{});const report={schemaVersion:1,patchId:'TDT-SPECTRAL-QMAP-02',state:'SPECTRAL_QMAP_02_SOURCE_BAKED_AWAITING_PACKAGED_GPU',counts,gates,productionPointerMutated:false,physicalGpuClaims:false,cpuProductComputeClaim:false,intermediateReadbackClaim:0,failCount:0};writeArtifact('TDT_SPECTRAL_QMAP_02_SOURCE_GATE.json',report);console.log(`TDT-SPECTRAL-QMAP-02 ${counts.PASS} PASS / ${counts.DEFERRED} DEFERRED / 0 FAIL`);
