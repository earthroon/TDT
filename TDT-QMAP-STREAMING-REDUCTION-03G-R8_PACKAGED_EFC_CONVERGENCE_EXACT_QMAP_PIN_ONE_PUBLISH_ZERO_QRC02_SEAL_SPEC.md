# TDT-QMAP-STREAMING-REDUCTION-03G-R8

## Packaged EFC Convergence Physical Closure / Canonical QMap Field Pin / QMap-QWave Device Identity Coherence / Bakemono Rinne Live Field Consumption / Converged Final Surface Publication / One-Publish Final Surface Receipt / Zero Hidden QRC02 Execution / No Product Promotion Yet Seal

## 0. Identity

```text
Patch ID = TDT-QMAP-STREAMING-REDUCTION-03G-R8
Short ID = QSR03G-R8
Parent = TDT-QMAP-STREAMING-REDUCTION-03G-R7
Umbrella = TDT-QMAP-STREAMING-REDUCTION-03
Specification state = SPEC_READY_AWAITING_SOURCE_BAKE
```

Required parent source state:

```text
SOURCE_BAKED_QMAP_STREAMING_REDUCTION_03G_R7_PACKAGED_8K_CORE_AUTHORITY_32026_WINDOW_PLAN_SEVENTY_TWO_CHUNK_GRAPH_FINAL_CHUNK_218_GEOMETRY_DERIVED_READBACK_STREAMING_QMAP01_ORACLE_FULL_OUTPUT_PARITY_TRACKED_RESOURCE_ALLOCATOR_NO_GLOBAL_FREQUENCY_OR_POWER_RESOURCE_THREE_CYCLE_PLATEAU_SIXTEEN_WAY_WARM_SHARING_NO_PRODUCT_PROMOTION_AWAITING_PACKAGED_8K_PHYSICAL_EXECUTION
```

R8 physically validates that the exact authentic QSR03 QMap field produced by the packaged 8K candidate composition is consumed by the complete Effect Field Convergence path.

```text
QSR03 8K execution
→ canonical AFT00 QMap publication
→ exact QMap graph pin
→ device-coherent QWave real and analytic fields
→ Depth / Alpha / Phase Gamma / Terminal R1C
→ Bakemono Rinne WGSL-06
→ one EFC graph submission and fence
→ one Surface Registry adoption
→ one Pipeline final publication
→ one final-surface receipt
```

R8 may consume R2 named evidence and R3-R7 qualification authorities. It may not issue a trial permit, physical admission, product promotion permit, or global QSR03 product route.

## 1. State ownership and SSOT

The canonical QMap field belongs to AFT00 Analysis Field Authority. The exact QSR03 target GPUTexture is adopted without a second QMap texture or copy.

The EFC coordinator owns one exact QMap graph pin. It may not resolve QMap again by semantic lookup after accepting the QSR03 delivery.

QWave real and analytic services own their execution and publication, but their GPU leases, fields and pins must use the same device epoch and device identity as the QMap pin and EFC graph.

One immutable EFC field-set capability owns the exact QMap, QWave-real and QWave-analytic pins for one unsubmitted graph. It cannot be cloned, rebound or reused.

Surface Registry owns one adopted converged texture record. Pipeline authority owns one final publication. The one-publish authority owns the idempotent publish key and final receipt.

QRC02 remains the installed global product authority. R8 does not replace it and must execute it zero times.

## 2. Source completion state

```text
SOURCE_BAKED_QMAP_STREAMING_REDUCTION_03G_R8_CANONICAL_AFT00_QMAP_FIELD_EXACT_QMAP_GRAPH_PIN_QMAP_QWAVE_DEVICE_IDENTITY_COHERENCE_DEPTH_ALPHA_PHASE_GAMMA_BAKEMONO_EFC_GRAPH_LIVE_PINNED_FIELD_CONSUMPTION_ONE_GRAPH_SUBMISSION_ONE_FINAL_SURFACE_ADOPTION_ONE_PIPELINE_PUBLICATION_ZERO_HIDDEN_QRC02_EXECUTION_NO_PRODUCT_PROMOTION_AWAITING_PACKAGED_EFC_PHYSICAL_EXECUTION
```

Physical completion may be claimed only after real packaged execution:

```text
PACKAGED_EFC_CONVERGENCE_PHYSICAL_BAKED_QMAP_STREAMING_REDUCTION_03G_R8_AUTHENTIC_QSR03_QMAP_PIN_DEVICE_COHERENT_QWAVE_REAL_AND_ANALYTIC_DEPTH_ALPHA_PHASE_GAMMA_BAKEMONO_CONVERGENCE_ONE_EFC_GRAPH_SUBMISSION_ONE_CONVERGED_FINAL_SURFACE_ONE_FINAL_SURFACE_RECEIPT_ZERO_HIDDEN_QRC02_EXECUTION_ZERO_PRIVATE_RESOURCE_LEAK_GLOBAL_QRC02_BRIDGE_UNCHANGED_NO_PRODUCT_PROMOTION
```

## 3. Canonical AFT00 QMap adoption

The QSR03 candidate composition publishes:

```text
semantic = tdt.analysis.qmap.normalized-response.v1
producer = tdt.analysis.producer.qmap.normalized-response
format = rgba16float
geometry = 7680 × 4320
coordinate space = output-pixel
claim level = EFFECTIVE_EXECUTION
candidateQualificationOnly = true
productRouteEligible = false
promotionPermitDigest = null
```

Required identity:

```text
QSR03 QMap target resource ID
= AFT00 QMap field resource ID
```

Forbidden adoption behavior:

```text
second QMap GPUTexture allocation
copyTextureToTexture for QMap adoption
CPU, WebGL or Canvas QMap copy
semantic-only field substitution
```

The adoption receipt binds the QSR03 streaming receipt, runtime closure, route receipt, candidate publication, target identity, tracked resource ID, R7 parity receipt and R7 physical receipt.

## 4. Exact QMap graph pin

The EFC coordinator acquires a dedicated graph pin from the exact AFT00 field returned by QSR03. The graph pin is distinct from the caller delivery pin.

The pin binds field ID, generation, resource ID, semantic, producer, source identity/revision, geometry, format, device epoch/identity and execution lineage.

Currentness is checked after acquisition, before QWave work, before EFC field-set binding, before Bakemono bind-group creation, immediately before submit and after the EFC fence.

```text
ACQUIRED
→ EFC_FIELD_SET_BOUND
→ EFC_GRAPH_SUBMITTED
→ EFC_GRAPH_FENCED
→ RELEASED
```

Semantic QMap re-query after pin acquisition is forbidden.

## 5. QWave device identity coherence

QWave real compatibility receives expected QMap field/generation and expected device epoch/identity. Its acquired GPU lease must match before allocation and submission.

```text
QWave real submissions/fences = 1/1
QWave real publications = 1
```

QWave analytic consumes exact pinned real/source fields and publishes imaginary and analytic fields together.

```text
QWave analytic submissions/fences = 1/1
compute passes = 2
published fields = 2
```

Required equality:

```text
QMap device epoch
= QWave real device epoch
= QWave analytic device epoch
= EFC graph device epoch

QMap device identity
= QWave real device identity
= QWave analytic device identity
= EFC graph device identity
```

QMap, QWave-real and QWave-analytic resources must be physically distinct.

## 6. Canonical EFC field set

One field-set capability contains the exact QMap, QWave-real and QWave-analytic pins, qualification Depth lineage, graph-local Alpha lineage, source identity/revision and device identity.

```text
ACQUIRED
→ BOUND
→ SUBMISSION_PENDING
→ FENCED
→ RELEASED
```

The receipt binds pin IDs, pinned resource IDs, acquisition/release sequence and graph binding digest. Rebinding, cloning or reuse is forbidden.

## 7. Complete EFC graph

```text
mode = CANONICAL_FINAL
purpose = QUALIFICATION_FINAL_SURFACE
Depth Field = ENABLED
Alpha Sensitivity = ENABLED
Phase Gamma Proof = ENABLED
Bakemono Rinne = ENABLED
```

Canonical order:

```text
TERMINAL_R1C
DEPTH_FIELD
ALPHA_SENSITIVITY
PHASE_GAMMA_PROOF
BAKEMONO_RINNE
```

```text
post-lowpass compute passes = 10
uniform allocations = 5
standalone uniform buffers = 0
recorder queue submissions = 0
intermediate readbacks = 0
```

All stages are recorded into one EFC command graph.

## 8. Bakemono Rinne live consumption

WGSL-06 binds:

```text
binding 0 = Phase Gamma base
binding 1 = exact pinned QSR03 QMap
binding 2 = exact pinned QWave real
binding 3 = Terminal Integrated R1C
binding 4 = Bakemono Rinne output
binding 5 = uniform-ring allocation
```

Caller QMap/QWave/effect textures and legacy DeltaE, mask or scalar textures are forbidden. Actual resource identity, not descriptor similarity, is authoritative.

```text
8K workgroups = 960 × 540 × 1
Bakemono live dispatches = 1
```

The consumption receipt binds graph, pipeline, shader, bind group, exact field pins, Terminal R1C, Phase Gamma, field set, dispatch and output resource.

## 9. EFC submission and aggregate accounting

The complete EFC graph owns one encoder, one command buffer, one queue submission and one completion fence. Pins remain current through that fence.

Aggregate fixture accounting:

```text
QSR03 8K = 72 submissions / 72 fences
QWave real = 1 / 1
QWave analytic = 1 / 1
EFC graph = 1 / 1
aggregate = 75 / 75
```

Surface adoption and Pipeline publication add zero GPU submissions. A seventy-sixth submission is forbidden.

## 10. Converged final candidate

```text
role = BAKEMONO_RINNE_PHASE_GAMMA_R1C_FINAL
semantic = tdt.surface.effect-field-converged.linear-premul.rgba16float.v1
format = rgba16float
geometry = 7680 × 4320
transfer = linear
alpha mode = premultiplied
coordinate space = output-pixel
```

The final lineage includes QSR03 execution/streaming, QWave real/analytic, Alpha, Phase Gamma, Terminal R1C, Bakemono dispatch/recording and EFC final receipts.

## 11. One Surface Registry adoption and one Pipeline publication

Exactly one canonical surface record is created for the exact converged GPUTexture. Its device identity matches the QMap/QWave/EFC field set.

The publish key binds operation, source revision, graph ID, convergence receipt, final descriptor and surface ID.

```text
Surface Registry registrations = 1
ownership adoptions = 1
Pipeline publishFinalCandidate calls = 1
publication-sequence increments = 1
final revisions = 1
final-surface receipts = 1
```

A duplicate publish attempt creates no surface, revision, listener publication or receipt.

Preview and Export consume the same Pipeline final revision and do not republish or rerun EFC/QMap.

## 12. Live convergence receipt

The QSR03 EFC convergence helper is wired into the live path after final publication. It receives actual QMap pin/execution/streaming/runtime receipts, QWave coherence, EFC field set, Bakemono dispatch, EFC final receipt, one-publish receipt and QRC02 denial receipt.

```text
promotionPermitDigest = null
qualificationOnly = true
qrc02RouteReentryCount = 0
callerSuppliedQmapTextureCount = 0
intermediateReadbackCount = 0
productPromotion = false
```

## 13. Zero hidden QRC02 execution

The global QRC02 bridge object identity remains unchanged. The EFC coordinator receives the isolated QSR03 candidate bridge explicitly and may not perform a global product-bridge lookup.

Required deltas:

```text
QRC02 ensure requests = 0
QRC02 cold jobs = 0
QRC02 queue submissions = 0
QRC02 QMap publications = 0
QRC02 warm deliveries = 0
QRC02 fallback invocations = 0
```

If candidate QSR03 fails, R8 fails. QRC02 fallback is forbidden.

## 14. Resource and validation closure

QMap, QWave-real and QWave-analytic graph pins release after the EFC fence. Graph-local Alpha, Phase Gamma, Terminal R1C and Bakemono intermediate resources retire. The final texture remains Pipeline-owned until final invalidation.

After shutdown:

```text
EFC-private GPUBuffer count = 0
EFC-private GPUTexture count = 0
Analysis pin count = 0
unpublished candidate count = 0
orphan Surface Registry records = 0
duplicate final publications = 0
```

All shader, validation, internal, OOM, uncaptured, device-loss, bind-group, encoding and submission error counts must be zero.

## 15. Evidence boundary

R8 defines O001-O080 as immutable qualification gates. They cover packaged route, canonical QMap, exact pin, QWave coherence, EFC field set and stage order, Bakemono binding, one graph submission, converged final surface, one-publish closure, zero QRC02, aggregate 75/75 accounting, resource retirement and no-promotion boundary.

The O receipts form a domain-separated 80-leaf Merkle tree outside the R2 P001-P096 tree. Later R2 executors may independently consume R8 evidence for P081-P088. R8 cannot create P receipts or admission/promotion authority.

## 16. Source gates and negative controls

```text
Source Gates = 336
Negative Controls = 144
Physical O-gate definitions = 80
```

Source controls cover AFT00 adoption, exact pins, QWave identity, immutable field set, complete EFC stages, live Bakemono resources, one submission/fence, final identity, Surface Registry, one-publish Pipeline closure, live convergence receipt, zero QRC02, aggregate accounting, retirement, validation, evidence isolation and parent A-R7 regressions.

Negative controls cover copied or substituted QMap fields, semantic re-query, stale/early-released pins, cross-device QWave, reused field sets, disabled/reordered stages, caller/legacy textures, wrong Bakemono resources/dispatch, extra EFC submissions, duplicate final surfaces/publications, detached convergence evidence, hidden QRC02 fallback, leaks, ignored errors, O/P tree mixing and false promotion claims.

## 17. Source completion facts

```text
Source Gates = 336/336
Negative Controls = 144/144
Physical O-gate definitions = 80
QSR03 submissions/fences = 72/72
QWave real submissions/fences = 1/1
QWave analytic submissions/fences = 1/1
EFC submissions/fences = 1/1
aggregate submissions/fences = 75/75
EFC post-lowpass passes = 10
Bakemono dispatches = 1
Surface Registry registrations = 1
Pipeline final publications = 1
QRC02 execution deltas = 0
physical O gates executed = 0/80
R2 P gates executed = 0/96
trial permit artifacts = 0
physical admission artifacts = 0
product permit artifacts = 0
product promotion = false
global product route = QRC02 unchanged
```

Source device doubles prove authority and call-graph closure only. They are not packaged physical EFC evidence.

## 18. Required physical facts later

```text
O gates = 80/80
real submissions/fences = 75/75
QMap graph pins = 1
QWave real pins = 1
QWave analytic pins = 1
Bakemono live dispatches = 1
converged surface registrations = 1
Pipeline final publications = 1
final-surface receipts = 1
QRC02 requests/submissions/publications = 0/0/0
terminal private resource balance = 0
WebGPU validation errors = 0
product promotion = false
```

## 19. Package policy

The code ZIP contains AFT00 adoption, exact pin authority, QWave coherence, Bakemono binding, EFC graph/surface/one-publish authority, QRC02 denial, O001-O080 definitions, source validators and physical launcher/verifier stubs.

It excludes this specification, generated O/P receipts, Merkle artifacts, trial permit, physical admission, product permit, reports, logs, temporary evidence, nested ZIPs and Git metadata.

The GitHub commit contains this specification only.

## 20. Next patch

```text
TDT-QMAP-STREAMING-REDUCTION-03G-R9

Named P001-P096 Physical Admission Closure /
Preliminary P001-P088 Merkle /
Signed Preliminary Admission /
One-Shot Qualification Trial Permit /
P089-P096 Promoted-Route Trial /
Final Ninety-Six-Leaf Merkle /
Signed Physical Admission /
No Product Promotion Permit Yet Seal
```

R9 may execute the complete R2 physical gate registry and create the signed physical-admission receipt. It may not issue the final product promotion permit.

## 21. Final seal

```text
R8 does not accept a field merely because it has the QMap semantic.

It adopts the exact QSR03 GPUTexture into AFT00 and pins the exact field and
generation for the EFC graph.

QWave real and analytic fields share the QMap device epoch and device identity.
Bakemono Rinne binds the actual pinned QMap and QWave resources.

Depth, Alpha, Phase Gamma, Terminal R1C and Bakemono remain in one converged
command graph and one EFC submission.

The final texture enters Surface Registry once and Pipeline publishes it once.
Preview and Export consume the same revision without republishing.

QRC02 remains globally installed but executes zero times.

R8 produces qualification evidence only.
It creates no physical admission or product promotion permit.
```
