# TDT-QMAP-STREAMING-REDUCTION-03G-R3

## Packaged 1080p Candidate Bring-Up / Real WebGPU Device and Queue Binding / WGSL Module and Pipeline Admission / Five-Chunk Command Graph / Five Covering Submissions / Final-Chunk Projection / Candidate Analysis Publication / Private Resource Retirement / Zero Validation Error Smoke Seal

## 0. Identity

```text
Patch ID = TDT-QMAP-STREAMING-REDUCTION-03G-R3
Short ID = QSR03G-R3
Parent = TDT-QMAP-STREAMING-REDUCTION-03G-R2
Umbrella = TDT-QMAP-STREAMING-REDUCTION-03
Required parent = SOURCE_BAKED_QMAP_STREAMING_REDUCTION_03G_R2_NAMED_96_GATE_REGISTRY_EXACT_ARTIFACT_ALLOWLIST_WRITE_ONCE_EVIDENCE_PUBLICATION_PER_GATE_RECEIPT_DIGEST_PRELIMINARY_88_LEAF_CLOSURE_RUN_BOUND_TRIAL_PERMIT_FINAL_96_LEAF_MERKLE_CLOSURE_SIGNED_ADMISSION_AND_PROMOTION_AUTHORITIES_NO_BOOLEAN_RECEIPT_TRUST_GLOBAL_QRC02_BRIDGE_UNCHANGED_AWAITING_PACKAGED_CANDIDATE_AND_PHYSICAL_EXECUTION
State = SPEC_READY_AWAITING_SOURCE_BAKE
```

QSR03G-R3 is the first QSR03 patch permitted to enter the real packaged Electron WebGPU runtime. It binds the R1 composition root to a browser-owned adapter, device and queue; admits four packaged WGSL modules and seven compute pipelines; executes the five 1080p chunk graphs; publishes one qualification-only Analysis QMap field; and retires all operation-private resources.

R3 does not prove QMAP01 numeric parity, 4K or 8K completion, repeated resource plateau, cancellation replay, device-loss recovery, full EFC convergence, P001-P096 physical admission, or product promotion.

## 1. Mandatory parent corrections

A real `GPUCommandEncoder` has no canonical `commandEncoder.log`. Runtime topology authority is replaced by an authority-owned wrapper and immutable shadow journal whose events correspond one-for-one with real WebGPU calls.

Fixture pipeline objects are forbidden in packaged candidate mode. R3 creates real shader modules, explicit bind-group layouts, pipeline layouts, compute pipelines and bind groups from package-closed WGSL sources.

QSR03F publication is completed as:

```text
streaming execution admission exactly once
→ QMap target PUBLICATION_ELIGIBLE
→ texture ownership transfer
→ Analysis field publication
→ current qmapHandle returned
```

Main owns packaged run admission and evidence. The isolated renderer owns all WebGPU objects. Raw GPU objects never cross IPC.

## 2. Completion states

```text
SOURCE_BAKED_QMAP_STREAMING_REDUCTION_03G_R3_PACKAGED_RENDERER_BOOTSTRAP_REAL_WEBGPU_DEVICE_AND_QUEUE_AUTHORITY_PACKAGED_FOUR_WGSL_MODULES_SEVEN_REAL_COMPUTE_PIPELINES_NATIVE_ENCODER_JOURNAL_FIVE_CHUNK_GRAPH_AUTHORITY_CANDIDATE_ANALYSIS_PUBLICATION_PRIVATE_RESOURCE_RETIREMENT_R2_NAMED_SMOKE_EVIDENCE_BOUND_AWAITING_PACKAGED_1080P_SMOKE
```

Physical smoke may claim only after real packaged execution:

```text
PACKAGED_SMOKE_BAKED_QMAP_STREAMING_REDUCTION_03G_R3_REAL_ADAPTER_DEVICE_QUEUE_ZERO_SHADER_COMPILATION_ERROR_SEVEN_PIPELINES_ADMITTED_1888_WINDOWS_FIVE_COMMAND_ENCODERS_FIVE_COMMAND_BUFFERS_FIVE_QUEUE_SUBMISSIONS_FIVE_COMPLETION_FENCES_FINAL_CHUNK_PROJECTION_ONE_CANDIDATE_ANALYSIS_PUBLICATION_PRIVATE_RESOURCES_RETIRED_ZERO_WEBGPU_VALIDATION_ERROR_GLOBAL_QRC02_BRIDGE_UNCHANGED_NO_PROMOTION_PERMIT
```

In both states:

```text
global product bridge = QRC02
candidate bridge = QSR03G-R3
product promotion permit = absent
R2 P001-P096 admission = incomplete
```

## 3. Authorities

```text
liveDeviceAuthorityId = dadum.qmap.live-webgpu-device-authority.qsr03g-r3
liveQueueAuthorityId = dadum.qmap.live-webgpu-queue-authority.qsr03g-r3
shaderAssetAuthorityId = dadum.qmap.packaged-shader-asset-authority.qsr03g-r3
pipelineAuthorityId = dadum.qmap.live-pipeline-authority.qsr03g-r3
encoderFactoryAuthorityId = dadum.qmap.live-command-encoder-factory.qsr03g-r3
commandGraphAuthorityId = dadum.qmap.command-graph-observation-authority.qsr03g-r3
queueObservationAuthorityId = dadum.qmap.queue-observation-authority.qsr03g-r3
validationObserverAuthorityId = dadum.qmap.webgpu-validation-observer.qsr03g-r3
finalEwaFixtureAuthorityId = dadum.final-ewa.packaged-smoke-fixture-authority.qsr03g-r3
candidatePublicationAuthorityId = dadum.qmap.candidate-analysis-publication-authority.qsr03g-r3
resourceRetirementAuthorityId = dadum.qmap.physical-resource-retirement-authority.qsr03g-r3
candidateSmokeAuthorityId = dadum.qmap.packaged-candidate-smoke-authority.qsr03g-r3
```

## 4. Packaged route isolation

Smoke requires packaged Electron, no development-server URL, an exact candidate mode, fresh external evidence root, authentic candidate permit, and unchanged QRC02 global bridge.

```text
candidate key = __DADUM_QMAP_RUNTIME_CANDIDATE_BRIDGE__
global key = __DADUM_QMAP_RUNTIME_BRIDGE__
```

R3 writes only the candidate key. Normal product windows, normal EFC, Preview, Export and project loading cannot invoke the smoke route.

## 5. Real WebGPU admission

The renderer requests:

```js
navigator.gpu.requestAdapter({
  powerPreference: "high-performance",
  forceFallbackAdapter: false
})
```

Required device limits include 1920 texture dimension, 256 compute invocations and workgroup X, workgroup Y 8, workgroups-per-dimension 7168, workgroup storage 16384, and sufficient buffer/storage-binding sizes for the QSR03B arena.

Actual adapter and device limits enter the plan and receipt. Missing WebGPU, rejected profile, insufficient limits, rejected device request or unexpected device loss fails smoke.

Validation, internal and out-of-memory scopes, uncaptured-error observation, shader compilation information and `device.lost` observation begin before shader creation. Every WebGPU error count must be zero.

## 6. Packaged WGSL and pipelines

One generated package-closed asset bundle contains exact UTF-8 source and SHA-256 for:

```text
final_ewa_periodic_hann_window_chunk_qsr03.wgsl
chunk_local_stockham64_qsr03.wgsl
chunk_local_power_reduction_qsr03.wgsl
qmap_compact_normalized_response_projection_qsr03.wgsl
```

Network, development-server and caller shader loading are forbidden.

Exactly seven compute pipelines are admitted:

```text
1 extraction / main
2 Stockham stage / stockham64_stage
3 transpose / transpose64
4 power first partial / power_and_first_partial
5 partial merge / merge_sixteen_partials
6 compact finalizer / finalize_and_scatter_compact
7 QMap projection / main
```

Explicit layouts verify binding numbers, resource classes, access modes, dynamic offsets, minimum sizes, visibility and rgba16float storage output. Fixture or mock pipeline markers are rejected.

## 7. Real encoder and topology authority

Each chunk owns one real command encoder and one private shadow journal. Admitted events are:

```text
BEGIN_COMPUTE_PASS
SET_PIPELINE
SET_BIND_GROUP
DISPATCH_WORKGROUPS
END_COMPUTE_PASS
FINISH_ENCODER
```

The wrapper records an event and invokes the corresponding real API in program order. The journal is immutable after finish. Second finish, post-finish events, caller journals and native private-field inspection are forbidden.

## 8. Final EWA smoke fixture

R3 begins at the Final EWA boundary. A deterministic package-bound 1920×1080 rgba16float texture is uploaded using the real queue and delivered through the Final EWA replay authority.

This upload is qualification source preparation, not CPU QMap computation. Product execution performs no frequency, power, compact or QMap readback.

## 9. Canonical 1080p plan

```text
analysis = 1920 × 1080
window = 64 × 64
stride = 32 × 32
grid = 59 × 32
windows = 1,888
chunk capacity = 448
chunks = 5
chunk window counts = 448, 448, 448, 448, 96
final chunk index = 4
```

Resources:

```text
QSR03B arena buffers = 10
QSR03B arena bytes = 66,758,400
compact records = 1,888
compact bytes = 105,728
QMap format = rgba16float
QMap bytes = 16,588,800
```

## 10. Five-chunk command graph

Chunks 0 through 3 contain extraction, Stockham/transpose and power/compact work:

```text
passes per chunk = 3
dispatches per chunk = 18
```

Chunk 4 appends projection in the same encoder:

```text
passes = 4
dispatches = 19
```

Aggregate:

```text
compute passes = 16
dispatches = 91
command encoders = 5
command buffers = 5
queue submissions = 5
completion fences = 5
maximum in-flight submissions = 1
```

A sixth projection encoder or submission is forbidden.

After each real fence, arena generation, compact range and source cursor advance exactly once. Final generation is 5, compact base is 1,888, source state is STREAMING_COMPLETE, compact state is FULLY_COMPLETED and QMap state is PUBLICATION_ELIGIBLE.

## 11. Candidate Analysis publication

Exactly one Analysis build lease exists. Intermediate chunks leave it BUILDING. QSR03F admits ordered streaming execution once, transfers the QMap texture once and publishes:

```text
semantic = tdt.analysis.qmap.normalized-response.v1
producer = tdt.analysis.producer.qmap.normalized-response
format = rgba16float
geometry = 1920 × 1080
coordinateSpace = output-pixel
claimLevel = EFFECTIVE_EXECUTION
candidateQualificationOnly = true
productRouteEligible = false
warmProductCacheEligible = false
```

The runtime returns a current `qmapHandle`, streaming receipt, five chunk records, five command-graph receipts, queue ledger, publication receipt and private-retirement receipt.

## 12. Resource retirement

After publication, the compact target, ten arena buffers and Final EWA fixture are retired. Invocation disposal must not destroy the Analysis-owned QMap.

After explicit candidate field release and root shutdown:

```text
candidate-owned GPUBuffer count = 0
candidate-owned GPUTexture count = 0
candidate Analysis field count = 0
active device lease count = 0
candidate bridge installed = false
```

Pipeline and bind-group references are released even though WebGPU exposes no explicit destroy method for those objects.

## 13. R2 evidence integration

R3 reuses R2 canonical JSON, exact allowlist, write-once publisher, file/self digests, executor verification, package closure and domain-separated Merkle rules.

R3 has a separate namespace:

```text
C001-C032
```

Main evaluates each named predicate from detailed renderer observations and emits one write-once receipt per gate plus one 32-leaf Merkle root. Renderer cannot submit a PASS decision.

C-gates are qualification smoke evidence only. They cannot enter the R2 P001-P096 tree or mint a trial, admission or product permit.

Gate families:

```text
C001-C008 packaged route, adapter, device and queue
C009-C016 four shader modules and seven pipelines
C017-C024 Final EWA, plan, arena, five command graphs/submissions/fences
C025-C032 final projection, completion, publication, retirement and zero errors
```

## 14. Stable error boundary

Stable errors cover packaged admission, candidate permit, global bridge mutation, WebGPU/adapter/device/limits, shader/layout/pipeline admission, native encoder private-log use, command graph and submission/fence mismatch, fixture/plan/resource mismatch, cursor/compact completion, duplicate Analysis admission, publication/handle/ownership failure, resource leaks, validation/internal/OOM/uncaptured errors, incomplete smoke evidence and product-promotion attempts.

No R3 error path installs QSR03 globally or invokes QRC02 through the candidate route.

## 15. Implementation surfaces

R3 adds live device/queue, packaged shader assets, pipeline authority, encoder journal, validation observer, Final EWA smoke fixture, candidate Analysis authority, candidate publication, physical retirement and live-root dependencies.

Electron/renderer additions include the isolated smoke BrowserWindow, IPC, exact allowlist, C-gate registry, evidence coordinator, finalizer, renderer bootstrap and renderer runner.

Mandatory parent edits remove `commandEncoder.log` authority, bind concrete pipeline/bind-group creation, return `qmapHandle`, prevent duplicate Analysis admission and preserve candidate-only evidence through disposal.

## 16. Source gates and negative controls

```text
Source Gates = 256
Negative controls = 104
candidate smoke gates = 32
```

Source gates cover authority isolation, real adapter/device APIs, four WGSL assets, seven pipeline definitions, encoder journal, 1080p geometry and arena, deterministic Final EWA replay, five-chunk graph, five submissions/fences, one publication, resource retirement, zero-error observation, R2 write-once evidence integration, no mocks and no promotion.

Negative controls cover global bridge mutation, unpackaged/dev execution, GPU IPC leakage, mock devices/queues, limit bypass, external WGSL, shader mutation, fixture pipelines, layout mismatch, native private-log use, journal forgery, wrong geometry/counts, extra resources, caller Final EWA injection, graph/submission mutations, pre-fence commitment, synthetic tickets, duplicate or early publication, resource leaks, ignored WebGPU errors, C/P gate mixing, smoke-based permit minting and false 4K/8K/promotion claims.

Source device doubles reproduce the WebGPU API call graph but are not physical evidence. Real Windows x64 packaged execution remains required for C001-C032.

## 17. Source completion facts

```text
Source Gates = 256/256
Negative controls = 104/104
packaged WGSL assets = 4
real pipeline definitions = 7
candidate smoke gate definitions = 32
native commandEncoder.log authority = absent
fixture pipelines physically admitted = false
1080p windows = 1,888
1080p chunks = 5
final chunk windows = 96
expected passes = 16
expected dispatches = 91
expected submissions = 5
expected fences = 5
product promotion = false
product permit artifacts = 0
R2 physical gates executed = 0/96
QRC02 product route = unchanged
```

Source simulation results must be reported separately from packaged physical smoke.

## 18. Required physical smoke facts later

```text
Candidate smoke gates = 32/32
shader modules = 4
compute pipelines = 7
command encoders = 5
command buffers = 5
queue submissions = 5
completion fences = 5
compute passes = 16
dispatches = 91
Analysis execution admissions = 1
candidate publications = 1
qmapHandle returned = true
QMap readbacks = 0
all WebGPU error counts = 0
private resource balance after field release = 0
global product promotion = false
product permit artifacts = 0
```

## 19. Prohibited claims

```text
QSR03_QMAP01_PARITY_PASS
QSR03_4K_PRODUCT_PASS
QSR03_8K_PRODUCT_PASS
QSR03_RESOURCE_PLATEAU_PASS
QSR03_CANCELLATION_REPLAY_PHYSICAL_PASS
QSR03_DEVICE_LOSS_RECOVERY_PHYSICAL_PASS
QSR03_EFC_END_TO_END_PASS
QSR03_PRODUCT_ROUTE_PROMOTED
PHYSICAL_QMAP_STREAMING_REDUCTION_03_PASS
```

## 20. Package and repository policy

The code ZIP contains live device/queue authority, packaged WGSL bundle, pipeline authority, explicit layouts, encoder journal, Final EWA smoke fixture, candidate publication and retirement, packaged renderer/Main integration, C001-C032 evidence definitions, validators and physical smoke launcher/verifier.

It excludes this specification, smoke evidence, C/P receipts, Merkle artifacts, preliminary admission, trial permit, physical admission, product permit, reports, logs, temporary configs, nested ZIPs and Git metadata.

The GitHub commit contains this specification only.

## 21. Next patch

```text
TDT-QMAP-STREAMING-REDUCTION-03G-R4

Packaged 4K Core Physical Closure /
QMAP01 Output Parity /
Eighteen-Chunk Submission Parity /
Fence-Covered Compact Completion /
Analysis Publication Authenticity /
Five-Cycle Resource Plateau /
Thirty-Two-Way Warm Sharing /
No Product Promotion Yet Seal
```

R4 may perform qualification-only QMap readback for parity. It may not issue the final product promotion permit.

## 22. Final seal

```text
R3 is the first QSR03 patch that must touch the real GPU.

A source device double is not physical evidence.
A fixture pipeline is not a compute pipeline.
A native private encoder field is not a command receipt.
A streaming receipt without a current qmapHandle is not candidate delivery.

The packaged renderer acquires the real adapter, device and queue.
Four packaged WGSL modules create seven pipelines.
Five encoders cover all 1,888 windows in five submissions and five fences.
The fifth encoder carries final projection.

The completed QMap transfers once to Analysis Field Authority.
The runtime returns one qualification-only qmapHandle.
Private resources retire and candidate teardown balances to zero.

C001-C032 are named, write-once and Merkle-sealed.
They cannot enter the product-promotion tree.

No numeric parity is claimed.
No product permit is minted.
The global QRC02 bridge remains unchanged.
```
